# Capability registry

Version 0.1 reserves these stable names:

`provider-health`, `model-listing`, `text-generation`, `text-streaming`, `structured-output`, `tool-calling`, `reasoning-events`, `embeddings`, `transcription`, `speech-generation`, `image-understanding`, `image-generation`, `image-editing`, `video-generation`, `video-editing`, and `agent-execution`.
