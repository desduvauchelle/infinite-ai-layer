# @infinite-ai/core

Provider-neutral TypeScript contracts, routing, policy checks, normalized events, and errors for Infinite AI Layer.

It provides the `AiClient`, public contracts, deterministic mock adapter, data-boundary checks, and normalized error model. The package is a 0.1 alpha.
