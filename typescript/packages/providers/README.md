# @infinite-ai/providers

Ollama and OpenAI-compatible TypeScript provider adapters for Infinite AI Layer.

It supports Ollama plus direct and gateway OpenAI-compatible connections. The package is a 0.1 alpha and requires `@infinite-ai/core`.
